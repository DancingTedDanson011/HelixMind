---
title: Autonome Modi
description: HelixMind automatisch scannen, fixen und verbessern lassen — Sicherheitsaudits, Bug-Fixes und mehr.
order: 6
category: CLI-Nutzung
---

# Autonome Modi

> **Login erforderlich (Free+):** Autonome Modi (`/auto`, `/security`, `/monitor`) erfordern ein kostenloses HelixMind-Konto. Fuehre `helixmind login` aus zum Freischalten — kostenlos, keine Kreditkarte noetig. Im Open Source Modus sind diese Befehle nicht verfuegbar.

HelixMind kann **autonom im Hintergrund** arbeiten, waehrend du weiter codest.

## Autonomer Scan (`/auto`)

```bash
/auto
```

Der Agent arbeitet Issues in Prioritätsreihenfolge ab:

1. **Kritische Bugs** — Crashes, Null-Pointer, unbehandelte Exceptions
2. **Sicherheitslücken** — Injection, XSS, hardcodierte Secrets
3. **Fehlende Fehlerbehandlung** — Uncaught Promises, unvalidierter Input
4. **Kaputte Tests** — Fehlschlagende Testfälle
5. **Performance-Bottlenecks** — N+1 Queries, unnötige Re-Renders
6. **Dead Code** — Ungenutzte Funktionen
7. **Code-Qualität** — Linting-Issues, Style-Inkonsistenzen

### Zielgerichteter Modus

```bash
/auto Refactore alle API-Endpoints auf die neue Validierungs-Middleware
```

### Stoppen

```bash
/stop     # Oder ESC ESC für Notfall-Stopp
```

## Sicherheits-Audit (`/security`)

```bash
/security
```

Führe einen **einmaligen Sicherheits-Audit** deiner Codebase durch:

| Kategorie | Prüfungen |
|-----------|-----------|
| **Injection** | SQL, Command, Template Injection |
| **Path Traversal** | Verzeichnis-Traversierung, Symlink-Attacken |
| **XSS** | Cross-Site Scripting, unsanierter Output |
| **Secrets** | Hardcodierte API-Keys, Passwörter, Tokens |
| **Dependencies** | Bekannte Schwachstellen, veraltete Pakete |
| **Validierung** | Fehlende Input-Validierung |
| **Auth** | Schwache Authentifizierung, fehlende Session-Verwaltung |

**Auto-Fix:** CRITICAL und HIGH Issues werden automatisch gefixt. MEDIUM und LOW werden gemeldet.

## Kontinuierlicher Security Monitor (`/monitor`)

```bash
/monitor [mode] [interval]
```

Starte **kontinuierliches Security-Monitoring** im Hintergrund:

| Modus | Beschreibung |
|:-----|:------------|
| `passive` | Erkennt Bedrohungen, ergreift aber keine Maßnahmen (nur Audit) |
| `defensive` | Führt vorab genehmigte Verteidigungsmaßnahmen automatisch aus |
| `active` | Fordert für jede Verteidigungsmaßnahme Genehmigung via Web-Dashboard an |

**Beispiele:**
```
/monitor                    # Passiv-Modus, 5 Min. Intervall
/monitor active 60         # Aktiv-Modus, 60 Sek. Intervall
/monitor defensive 180     # Defensiv-Modus, 3 Min. Intervall
```

**Features:**
- Echtzeit-Threat-Erkennung (Bruteforce, Portscan, Malware, Konfig-Änderungen)
- Automatisierte Verteidigungen (IPs blockieren, Prozesse beenden, Ports schließen)
- Web-Dashboard-Integration (`/dashboard/monitor`)
- Approval-Queue für manuelle Review (Aktiv-Modus)
- Baseline-System-Scanning und Drift-Erkennung

Siehe den [Security Monitor](/docs/security-monitor) Guide für vollständige Details.

## Wie Hintergrund-Sessions funktionieren

1. Neue Session wird mit isoliertem State erstellt
2. Du behältst dein **interaktives Prompt**
3. Hintergrund-Session erscheint als **Tab** im Terminal
4. **Benachrichtigungen** bei Abschluss oder gefundenen Issues
5. Sessions schließen automatisch nach **10 Sekunden**

## Session-Verwaltung

```bash
/sessions              # Alle Sessions auflisten
Ctrl+PageUp/PageDown   # Zwischen Tabs wechseln
/stop                  # Hintergrund-Sessions stoppen
ESC ESC                # Notfall-Stopp aller Sessions
```
