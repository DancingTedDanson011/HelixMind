apiVersion: v1
kind: Service
metadata:
  name: {{ include "helixmind.fullname" . }}
  labels:
    {{- include "helixmind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helixmind.selectorLabels" . | nindent 4 }}
